'use strict';

const sum = (number) => {
    let result = 0;
    const plus = number;
    function save() {
        result += plus;
        return result;
    }
};
const sum = fn();
console.log(sum(5));
console.log(sum(3));

